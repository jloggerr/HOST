#!/usr/bin/python
import socket

print ""
   HOST Scanner
      ""

r = str(raw_input("IP >>> ")
t = socket.gethostbyname(r)
s = socket.getfdn(r)

print ""
IP => %s
HOST => %s
      ""% (t, s)
      
